import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:alloc/app/modules/extrato/extrato_page.dart';

void main() {
  testWidgets('ExtratoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ExtratoPage(title: 'Extrato')));
    //  final titleFinder = find.text('Extrato');
    //  expect(titleFinder, findsOneWidget);
  });
}
