import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:alloc/app/modules/extrato/extrato_controller.dart';
import 'package:alloc/app/modules/extrato/extrato_module.dart';

void main() {
  initModule(ExtratoModule());
  // ExtratoController extrato;
  //
  setUp(() {
    //     extrato = ExtratoModule.to.get<ExtratoController>();
  });

  group('ExtratoController Test', () {
    //   test("First Test", () {
    //     expect(extrato, isInstanceOf<ExtratoController>());
    //   });

    //   test("Set Value", () {
    //     expect(extrato.value, equals(0));
    //     extrato.increment();
    //     expect(extrato.value, equals(1));
    //   });
  });
}
