import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:alloc/app/modules/carteira/provento/provento_controller.dart';
import 'package:alloc/app/modules/carteira/carteira_module.dart';

void main() {
  initModule(CarteiraModule());
  // ProventoController provento;
  //
  setUp(() {
    //     provento = CarteiraModule.to.get<ProventoController>();
  });

  group('ProventoController Test', () {
    //   test("First Test", () {
    //     expect(provento, isInstanceOf<ProventoController>());
    //   });

    //   test("Set Value", () {
    //     expect(provento.value, equals(0));
    //     provento.increment();
    //     expect(provento.value, equals(1));
    //   });
  });
}
