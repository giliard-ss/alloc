import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:alloc/app/modules/carteira/pages/ativo/ativo_controller.dart';
import 'package:alloc/app/modules/carteira/carteira_module.dart';

void main() {
  initModule(CarteiraModule());
  // AtivoController ativo;
  //
  setUp(() {
    //     ativo = CarteiraModule.to.get<AtivoController>();
  });

  group('AtivoController Test', () {
    //   test("First Test", () {
    //     expect(ativo, isInstanceOf<AtivoController>());
    //   });

    //   test("Set Value", () {
    //     expect(ativo.value, equals(0));
    //     ativo.increment();
    //     expect(ativo.value, equals(1));
    //   });
  });
}
